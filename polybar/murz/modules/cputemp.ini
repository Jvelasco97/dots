
[module/cputemp]
type = custom/script

format = <label>

exec = sensors | awk '/^Package/ {print $4}'
tail = true

format-background = ${colors.bg}
format-foreground = ${colors.orange}
format-padding = 1
format-prefix-foreground = ${colors.fg-alt}
label = %{T2} %output%%{T-}

